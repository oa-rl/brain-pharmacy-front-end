import { RowSelectedDirective } from './row-selected.directive';

describe('RowSelectedDirective', () => {
  it('should create an instance', () => {
    const directive = new RowSelectedDirective();
    expect(directive).toBeTruthy();
  });
});
